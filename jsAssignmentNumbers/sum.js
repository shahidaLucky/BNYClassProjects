/**
 * 
 */
let intArray= [];


function total(intArray) {
  if(!Array.isArray(intArray)) return;
  let totalNumber = 0;
  for (let i=0,l=intArray.length; i<l; i++) {
     totalNumber+=arr[i];
  }
  return totalNumber;
}
function total(arr);